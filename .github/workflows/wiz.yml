name: 'wiz-cli'
on: 
  pull_request:
  push:
    branches:
      - main

jobs:
  wiz-cli-iac-scan:
    name: 'Wiz-cli IaC Scan'
    runs-on: ubuntu-latest
    env:
      SCAN_PATH: "./aws" # Set the relative path in the repo to scan
      POLICY: "iac-fail-low" # Set the desired Wiz-cli policy to use

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash

    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Check out repository
      uses: actions/checkout@v3

    - name: Download Wiz-cli
      run: curl -o wizcli https://wizcli.app.wiz.io/latest/wizcli && chmod +x wizcli

    - name: Authenticate to Wiz
      run: ./wizcli auth --id "$WIZ_CLIENT_ID" --secret "$WIZ_CLIENT_SECRET"
      env:
        WIZ_CLIENT_ID: ${{ secrets.WIZ_CLIENT_ID }}
        WIZ_CLIENT_SECRET: ${{ secrets.WIZ_CLIENT_SECRET }}
    
    - name: Add Problem Matcher
      run: echo "::add-matcher::.github/wizcli-problem-matcher.json"

    - name: Run wiz-cli IaC scan
      run: RESULTS=$(./wizcli iac scan --path ./aws --policy "scotty-iac-monitor-only" --name scotty-gh-iac-scan --detailed > wizcli-results.json && python3 convert-to-sarif.py > results.sarif && cat results.sarif | jq .)

    # - uses: actions/upload-artifact@v3
    #   with:
    #     name: Output SARIF REPORT
    #     path: results.sarif

    - uses: github/codeql-action/upload-sarif@v2
      with:
        # Path to SARIF file relative to the root of the repository
        sarif_file: results.sarif

    - name: Outputs
      if: always()
      run: echo "::set-output name=results::$RESULTS"
